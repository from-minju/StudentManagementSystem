spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create #애플리케이션 실행 시점에 테이블을 drop하고 다시 생성한다.
#    properties:
#      hibernate:
##        show_sql: true #System.out으로 출력됨.
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug #JPA나 Hibernate가 생성하는 SQL이 다 보인다. log를 통해 출력됨.

